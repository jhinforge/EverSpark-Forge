# Runtime

Health checks, process management, hardware discovery, and logging.

- `Hardware/` contains NVIDIA GPU/CUDA discovery and Forge GPU assignment.
- `System/` contains host-platform and apt package helpers.
- `Logging/` contains the shared structured logging layer.
- `Managed/` installs pinned external runtimes and safely supervises local services.

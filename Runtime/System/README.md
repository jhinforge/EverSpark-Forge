# System

Shared host checks and package installation helpers migrated from the WebUI
branch baseline.

The current execution baseline supports Linux x86_64. Package installation is
kept separate from read-only diagnostics and still requires explicit root
execution.
